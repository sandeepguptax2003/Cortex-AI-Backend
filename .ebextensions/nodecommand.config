# No special config needed — Node.js version is set by the platform branch selection.
# Environment variables (PORT, NODE_ENV, etc.) are configured in the EB console.
option_settings:
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx
